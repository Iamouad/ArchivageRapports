@model IEnumerable<realMiniProjet.Models.Entities.Filiere>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom_filiere)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom_filiere)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_filiere }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_filiere }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_filiere })
        </td>
    </tr>
}

</table>
